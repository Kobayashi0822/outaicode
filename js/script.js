function copy() {
    var targetCode = document.getElementById("code"); 
    targetCode.select();                              
    document.execCommand("Copy");                     
}

function copy2() {
    var targetCode = document.getElementById("code2"); 
    targetCode.select();                              
    document.execCommand("Copy");                     
}